version: 2.0
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk
      # - image: mcr.microsoft.com/java/jdk:11u3-zulu-centos
    steps:
      - checkout
      - run:
          name: test
          command: |
            curl https://raw.githubusercontent.com/paulp/sbt-extras/master/sbt > sbt && chmod +x sbt
            ./sbt +test +publishM2
